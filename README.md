🚀 Telegram Task Redirector (Mini App)
Легкое и быстрое веб-приложение (TWA) для автоматического трекинга и перенаправления пользователей в Telegram боты или чаты с предустановленными сообщениями.

⚡ Технические особенности
Native Integration: Автоматическая подстройка под цветовую схему Telegram (Dark/Light режимы).

Haptic Feedback: Использование виброотклика (Taptic Engine) при успешных запросах и ошибках для улучшения UX.

Asset Performance: Оптимизированная предзагрузка анимаций из локальной директории для исключения задержек.

Security: Передача initData на бэкенд для валидации подлинности запроса.

Error Handling: Встроенная система повторных попыток и визуальное информирование об ошибках сети.

🛠 Стек технологий
Frontend: Чистый JavaScript (ES6+), HTML5, CSS3.

API: Telegram WebApp JS SDK.

Backend: Интеграция через REST API (JSON).

📂 Структура проекта
Plaintext

├── index.html          # Логика приложения и интерфейс
└── assets/             # Статические ресурсы
    ├── mail-box.gif    # Анимация процесса загрузки
    └── error.gif       # Анимация при сбое или отсутствии параметров
🚀 Быстрый старт
1. Настройка окружения
Убедитесь, что ваш хостинг поддерживает HTTPS, так как Telegram WebApp не работает с незащищенными протоколами.

2. Конфигурация API
В файле index.html укажите адрес вашего обработчика:

JavaScript

const BACKEND_URL = 'https://your-api-domain.com';
3. Запуск
Разместите файлы на сервере и подключите URL к вашему боту через Inline Keyboard или Menu Button. Для корректной работы приложение ожидает наличие start_param.

⚙️ Оптимизация для Production
Для максимальной скорости работы в мобильных сетях рекомендуется настроить кэширование статики на стороне Nginx:

Nginx

location /assets/ {
    expires 1y;
    add_header Cache-Control "public, no-transform";
}
Важно: Приложение использует tgWebAppStartParam для идентификации задания. Если параметр отсутствует, пользователь увидит экран ошибки с предложением вернуться в чат.

Желаю успешного запуска! Если понадобится добавить специфическую логику валидации — дай знать.
